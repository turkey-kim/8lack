import axios from 'axios';
import {SERVER_URL, USER_DEFAULT_IMG} from '../constant';
import {authHeaders} from './auth';

export const getUsers = async () => {
  //   try {
  //     //수정
  //     await axios.get(`${SERVER_URL}/`, {headers: authHeaders});
  //   } catch (err: any) {
  //     console.log(err);
  //   }
};
